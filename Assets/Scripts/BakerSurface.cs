using UnityEngine;
using UnityEngine.AI;
using System.Collections;
using System.Collections.Generic;

public class BakerSurface : MonoBehaviour
{
    
    public Component component;
    public void Awake()
    {
        
        
        // var c=GetComponents(typeof(Component));
        // foreach(var cd in c){
        //     Debug.Log(cd.GetType().ToString());
        // }
        
        
    }
}
