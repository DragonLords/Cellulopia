using System.Collections;
using System.Collections.Generic;
using System.Threading.Tasks;
using UnityEngine;

public class AStarMove : MonoBehaviour
{
    GameObject TemplateSquare;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    async Task Test(){
        await Task.Yield();
    }
}
